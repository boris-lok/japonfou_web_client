import { ScrollStartDirective } from './scroll-start.directive';

describe('ScrollStartDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollStartDirective();
    expect(directive).toBeTruthy();
  });
});
